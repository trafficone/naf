//is this supported through an external lib?
/*
    File-specific: (may move to files.rs)
    - open input/output files (handled by Rust?)
    - make temp prefix
    - close input/output files & set stat (???)
    */
